<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- US7/US11: local notification permission (required on Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- US8: read calendar events may be needed on some devices -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Debugz">

        <activity
            android:name=".view.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.MainActivity"
            android:exported="false"
            android:label="Discover Events" />

        <activity
            android:name=".view.EventDetailActivity"
            android:exported="false"
            android:label="Event Details" />

        <activity
            android:name=".view.MyEventsActivity"
            android:exported="false"
            android:label="My Events" />

        <activity
            android:name=".view.OrganizerDashboardActivity"
            android:exported="false"
            android:label="Organizer Dashboard" />

        <activity
            android:name=".view.EditEventActivity"
            android:exported="false"
            android:label="Event Form" />

        <activity
            android:name=".view.AttendeesActivity"
            android:exported="false"
            android:label="Attendees" />

        <activity
            android:name=".view.SignupActivity"
            android:exported="false"
            android:label="Sign Up" />

        <activity
            android:name=".view.AdminDashboardActivity"
            android:exported="false"
            android:label="Admin Dashboard" />

        <activity
            android:name=".view.AddFriendActivity"
            android:exported="false"
            android:label="Add Friend" />

        <activity
            android:name=".view.FriendsEventsActivity"
            android:exported="false"
            android:label="Friends Events" />

    </application>

</manifest>