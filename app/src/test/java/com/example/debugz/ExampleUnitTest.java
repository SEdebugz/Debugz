package com.example.debugz;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Provides the default placeholder host-side unit test generated for the Android project.
 * Its current role is mainly to confirm that the local unit-test harness is wired up.
 * Outstanding issues: it remains a template smoke test and does not validate any app
 * behavior beyond basic JUnit execution.
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
